#include <stdlib.h>
#include <stdio.h>


int main(){
    List *L;
    int tam, vert, x, y, i;
    scanf("%d", &vert);
    scanf("%d", &tam);
    i = 0;
    while(i < tam){
        scanf("%d%d", &x, &y);
        i++;
    }
    return 0;
}
